# BLOB Detection Backend Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies
numpy>=1.24.0
pillow>=10.0.0
opencv-python>=4.8.0

# Web server (FastAPI for async support)
fastapi>=0.109.0
uvicorn>=0.27.0

# ==============================================
# GPU Acceleration (Optional)
# ==============================================
# These packages provide GPU acceleration for image processing.
# The server will automatically detect and use the best available backend.
#
# CUDA (Windows/Linux with NVIDIA GPU):
#   pip install cupy-cuda12x   # For CUDA 12.x
#   pip install cupy-cuda11x   # For CUDA 11.x
#
# Metal/MPS (macOS with Apple Silicon):
#   pip install torch kornia
#
# The GPU backends are optional - the server will fall back to CPU (OpenCV)
# if no GPU packages are installed.
