# =============================================================================
# Follicle Labeller Environment Configuration
# =============================================================================
# Copy this file to .env and configure the variables for your environment.
#
# For Electron builds: Most settings are handled internally, only override if needed.
# For Web builds: Configure VITE_* variables for the frontend.
# For GPU Server: Configure server-side variables for the Python backend.
# =============================================================================

# -----------------------------------------------------------------------------
# FRONTEND CONFIGURATION (Vite - Web builds only)
# -----------------------------------------------------------------------------
# These variables are embedded at build time for web deployments.
# They must be prefixed with VITE_ to be accessible in the browser.

# Backend API URL - URL of the GPU server running blob_server.py
# Local development:
VITE_BACKEND_URL=http://127.0.0.1:5555
# Remote GPU server:
# VITE_BACKEND_URL=http://gpu-server.local:5555
# Production with HTTPS:
# VITE_BACKEND_URL=https://api.yourserver.com

# -----------------------------------------------------------------------------
# BACKEND CONFIGURATION (Python FastAPI Server)
# -----------------------------------------------------------------------------
# These are read by blob_server.py when running the GPU backend.

# Server binding
BLOB_SERVER_HOST=127.0.0.1
BLOB_SERVER_PORT=5555

# For web deployment, bind to all interfaces:
# BLOB_SERVER_HOST=0.0.0.0

# -----------------------------------------------------------------------------
# GPU CONFIGURATION
# -----------------------------------------------------------------------------
# CUDA device selection (for multi-GPU systems)
# Use comma-separated indices for multiple GPUs: 0,1,2
CUDA_VISIBLE_DEVICES=0

# Force CPU mode (set to 1 to disable GPU even if available)
# FORCE_CPU=0

# -----------------------------------------------------------------------------
# STORAGE PATHS (Optional - defaults to ~/.follicle-labeller/)
# -----------------------------------------------------------------------------
# Override default storage locations for models and projects

# Directory for trained YOLO models
# MODELS_DIR=/path/to/models

# Directory for saved projects (web mode only)
# PROJECTS_DIR=/path/to/projects

# Directory for temporary datasets during training
# TEMP_DATASETS_DIR=/path/to/temp

# -----------------------------------------------------------------------------
# TENSORRT CONFIGURATION (Optional)
# -----------------------------------------------------------------------------
# TensorRT optimization settings for inference acceleration

# Default precision for TensorRT export (fp16 recommended for consumer GPUs)
# TENSORRT_PRECISION=fp16

# Default image size for TensorRT engines
# TENSORRT_IMGSZ=640

# Default batch size for keypoint TensorRT engines
# TENSORRT_BATCH_SIZE=16

# -----------------------------------------------------------------------------
# LOGGING CONFIGURATION (Optional)
# -----------------------------------------------------------------------------
# Control logging verbosity

# Log level: DEBUG, INFO, WARNING, ERROR
# LOG_LEVEL=INFO

# Enable debug mode for development
# DEBUG=false

# -----------------------------------------------------------------------------
# CORS CONFIGURATION (Backend - for web deployments)
# -----------------------------------------------------------------------------
# By default, CORS allows all origins (*). For production, restrict to your domain.

# Allowed origins (comma-separated list)
# CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# -----------------------------------------------------------------------------
# DOCKER DEPLOYMENT
# -----------------------------------------------------------------------------
# When using docker-compose.gpu.yml, these can be set in docker-compose.override.yml
# or passed as environment variables to the container.
#
# Example docker-compose.override.yml:
# services:
#   backend:
#     environment:
#       - CUDA_VISIBLE_DEVICES=0
#       - BLOB_SERVER_HOST=0.0.0.0
# -----------------------------------------------------------------------------
